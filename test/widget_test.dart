import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Finora smoke test', (WidgetTester tester) async {
    // Placeholder test — Finora requires Hive initialization
    expect(1 + 1, 2);
  });
}
